# 마틴게일-스파이더 자동매매 봇 — 구현 명세 (SPEC.md)
---

## 0. 역할과 목표

너는 암호화폐 파생상품 자동매매 시스템을 만드는 시니어 백엔드 엔지니어다.
목표는 첨부된 엑셀(`제까깟_마틴게이_REALFINALBOSS.xlsx`)에 정의된 **마틴게일식 격자 분할진입 전략**을,
OrangeX(또는 Echobit) USDT 무기한 선물에서 실행하는 봇으로 구현하는 것이다.

**절대 원칙: 이 봇은 실제 돈을 잃을 수 있다. 추측으로 구현하지 마라.**
- API 스펙이 불명확하면 구현하지 말고 **질문하라**.
- 전략 규칙이 애매하면 임의 해석하지 말고 **선택지를 제시하고 질문하라**.
- "아마 이럴 것이다"로 작성한 코드는 금지. 모르는 건 `TODO(질문):` 주석으로 남기고 보고하라.

---

## 1. 기술 스택

- Python 3.11+ / `uv` 또는 `poetry`
- 비동기: `asyncio` + `httpx` + `websockets`
- 정밀도: **모든 가격·수량·금액 계산은 `Decimal`**. `float` 사용 금지 (반올림 오차가 곧 손실)
- 테스트: `pytest`, 커버리지 90%+ (계산 엔진은 100%)
- 설정: `pydantic-settings` + `.env` (키는 절대 코드/로그에 노출 금지)
- 로깅: `structlog` (JSON 라인), 모든 주문 요청/응답 원문 저장
- 알림: 텔레그램 봇 (선택, 인터페이스만 두고 구현은 나중)

---

## 2. Phase 구성 — 반드시 순서대로, 각 단계 끝에서 멈추고 보고

### Phase 0 — API 조사 (코드 작성 전)
1. OrangeX 공개 API 문서(`https://openapi-docs.orangex.com/`)를 읽고 `docs/api-notes.md`에 정리하라.
   - 인증 방식 (JSON-RPC 2.0 기반, Deribit 계열로 보임 — 서명 알고리즘·타임스탬프·nonce 규칙 확인)
   - 필요한 엔드포인트: 심볼/계약 스펙 조회, 잔고 조회, 포지션 조회, 지정가 주문, 주문 취소, 주문 조회, 레버리지 설정, 스톱 주문(SL/TP) 등록, 체결 스트림(WebSocket)
   - **계약 스펙**: 최소 주문 수량, 수량 스텝(lot size), 가격 틱, 최소 명목가치(min notional)
   - 레이트 리밋 수치
   - 테스트넷/데모 환경 존재 여부
2. 문서에서 확인 불가한 항목은 목록으로 만들어 보고하라. **추측해서 채우지 마라.**
3. `ccxt`가 이 거래소를 지원하는지 `python -c "import ccxt; print('orangex' in ccxt.exchanges)"` 로 확인.
   지원하면 ccxt 사용, 아니면 직접 클라이언트 작성.

**Phase 0 산출물**: `docs/api-notes.md`, 미확인 항목 목록. → 여기서 멈추고 확인받아라.

### Phase 1 — 전략 계산 엔진 (거래소와 완전 분리)
거래소 코드 없이, 순수 함수로 된 계산 엔진을 먼저 만든다.

**격자 정의**
- 총 100단계 = 대단계 5 × 소단계 20
- 진입가: 롱은 `base_price - tick*i`, 숏은 `base_price + tick*i` (i = 0..99)
  - BTC tick = 50 USDT, ETH tick = 2.5 USDT (설정값)
- **가중치는 재유도하지 말고 엑셀 E열 100개 값을 그대로 `config/weights.csv`로 추출해서 쓴다.**
  (참고: 1차 10→29 공차1, 2차 30→68 공차2, 3차 70→146 공차4, 4차 148→300 공차8, 5차 304→608 공차16)
  - 로드 시 `sum(weights) == 17130` 을 assert 하라.
- 단계별 증거금 = `round(equity * w_i / sum(w), 1)` USDT
- 단계별 수량 = `증거금 * leverage / 진입가`
- 평단가 = `누적증거금 * leverage / 누적수량`
- 예상 청산가(롱) = `(평단*누적수량 - equity) / (누적수량 * (1 - maint_margin_rate))`
- 목표 익절: ROE 기준 — 1~2차 10%, 3~4차 5%, 5차 2% → 익절가 = `평단 * (1 ± ROE/leverage)`
- 스톱로스 = `평단 * (1 ∓ 0.03)`

**골든 테스트 (필수)**
엑셀의 4개 시트, 각 100행의 컬럼(진입가/증거금/수량/누적수량/평단가/청산가/익절가/SL)을
CSV로 추출해 `tests/golden/` 에 넣고, 계산 엔진 출력이 **소수점 오차 범위 내에서 전부 일치**하는지 검증하라.
하나라도 안 맞으면 다음 Phase로 넘어가지 마라.

**추가로 반드시 계산해서 보고할 것 (엑셀에 이미 문제가 있다)**
1. `가용잔고 = equity - 누적증거금 + 누적수량*(현재가 - 평단)` 이 음수가 되는 최초 단계.
   (롱 시트 기준 5차 12단계에서 -192 USDT → **후반 9개 단계는 물리적으로 체결 불가**)
   → 봇은 이 지점을 사전에 계산해 **실행 가능한 최대 단계(`max_feasible_step`)를 시작 시 산출**하고,
   그 이후 단계는 격자에 아예 걸지 않는다.
2. 각 단계별 명목가치가 거래소 최소 주문 수량/최소 명목가치를 밑도는 단계 목록.
   (1차 1단계는 증거금 5.8 USDT × 20배 = 명목 116 USDT, BTC 0.0018개 — 거래소 최소 단위 미달 가능성 높음)
   → 미달 단계 처리 정책을 3가지 제시하고 나에게 선택하게 하라 (스킵 / 다음 단계에 합산 / 봇 기동 거부).
3. 수수료(taker 0.05%, maker 0.02%)와 8시간 주기 펀딩비를 반영했을 때
   각 대단계의 실질 손익분기 가격. 엑셀은 이걸 전혀 반영하지 않았다.

**Phase 1 산출물**: `strategy/` 패키지, 골든 테스트 통과, 위 3개 항목 보고서. → 멈추고 확인받아라.

### Phase 2 — 거래소 어댑터
1. 추상 인터페이스 `ExchangeAdapter` 정의 (get_balance, get_position, place_limit_order,
   place_stop_order, cancel_order, get_open_orders, watch_fills, get_contract_spec, set_leverage)
2. `PaperAdapter` 를 **먼저** 구현 — 인메모리 체결 시뮬레이터. 수수료·슬리피지·부분체결 반영.
3. `OrangeXAdapter` 구현. 서명 로직은 단위 테스트로 검증.
4. 모든 주문에 `client_order_id`(UUID) 부여 → **재시도 시 중복 주문 방지**.

### Phase 3 — 실행 엔진 (상태 머신)
사이클 상태: `IDLE → SCOUTING(진입조건 대기) → LADDERING(격자 운용) → TP_PENDING → CLOSING → COOLDOWN`

핵심 규칙:
- 진입 조건 필터: 일봉 RSI(14) ≤ 30 (롱) / ≥ 70 (숏), 또는 ATR 급등 시 격자 간격 확대.
  **RSI/ATR 계산은 라이브러리(`pandas-ta` 등) 사용하고 값 검증 테스트를 작성하라.**
- 격자 지정가 주문은 한 번에 전부 걸지 말고, **현재가 기준 앞쪽 N개(기본 5개)만 유지**하고 롤링으로 갱신.
  (100개 동시 주문은 레이트 리밋·미체결 관리 측면에서 위험)
- 체결될 때마다 평단/청산가/익절가 재계산 → 기존 TP 주문 취소 후 재등록 (취소-확인-등록 순서 엄수)
- **3차 이상 진입 시**: 평단(본절) 도달하면 보유 물량의 50%를 즉시 시장가 청산 (하이브리드 리셋)
- **4~5차 진입 시**: 평단 -3%(롱)에 거래소 측 스톱로스 주문을 **반드시** 등록. 등록 실패 시 전량 청산 후 봇 정지.
- 사이클 종료(전량 청산) 후 `COOLDOWN` (기본 30분) 동안 재진입 금지

**재시작 복구**: 봇을 재시작하면 로컬 상태를 믿지 말고
**거래소의 실제 포지션·미체결 주문을 조회해 상태를 재구성**하라. 불일치 시 자동 진행하지 말고 정지 후 알림.

### Phase 4 — 리스크 가드 (이건 협상 불가)
아래는 전부 필수 구현이며, 하나라도 빠지면 Phase 5로 넘어가지 마라.

| 가드 | 동작 |
|---|---|
| `TRADING_MODE` | 기본값 `paper`. `live`는 환경변수 + 실행 시 대화형 확인 2단계를 통과해야만 동작 |
| 킬 스위치 | 일일 손실이 설정한 한도(예: equity의 X%) 초과 시 전량 청산 후 프로세스 종료 |
| 청산가 근접 경보 | 현재가가 예상 청산가 대비 설정 % 이내 진입 시 즉시 알림 + 신규 진입 중단 |
| 최대 단계 제한 | `max_feasible_step` 및 사용자가 정한 `max_stage`를 넘는 진입 금지 |
| 잔고 재검증 | 매 진입 직전 실제 가용 증거금을 조회해 부족하면 진입 중단 (계산값만 믿지 않는다) |
| API 키 권한 | 출금 권한 없는 키만 사용. 기동 시 키 권한 확인 가능하면 확인 |
| 하트비트 | WebSocket 끊김 30초 이상 시 신규 진입 중단, 기존 TP/SL은 거래소 측에 남아있는지 확인 |
| 이상치 방어 | 가격이 직전 대비 비정상 급변(예: 5% 초과) 시 주문 중단하고 알림 |

### Phase 5 — 백테스트 & 페이퍼
1. 과거 1분봉(최소 2년, 2021년 5월·2022년 6월·2024년 8월 급락 구간 포함)으로 백테스트.
   출력: 사이클 수, 승률, 최대 손실, **청산 발생 횟수**, 수수료·펀딩비 총액, MDD.
2. 페이퍼 모드로 최소 2주 무중단 운영 로그를 남기는 스크립트/문서 제공.
3. 백테스트 결과 요약을 `docs/backtest.md`에 정리.

---

## 3. 작업 방식

- 각 Phase 끝에서 **반드시 멈추고** 결과를 요약해 보고할 것. 승인 없이 다음 Phase로 가지 마라.
- 테스트 먼저 작성(TDD). 계산 엔진은 테스트 없이 코드 작성 금지.
- 커밋은 작게, 메시지는 명확하게.
- 모르는 것·가정한 것은 매 보고마다 "가정 목록"으로 명시하라.
- 라이브 주문을 실제로 전송하는 코드는 내가 명시적으로 요청하기 전까지 실행하지 마라.

---

## 4. 내가 채워야 하는 설정값

```env
EXCHANGE=orangex
TRADING_MODE=paper
API_KEY=
API_SECRET=
SYMBOL=BTC-USDT-PERP
DIRECTION=long
EQUITY_USDT=10000
LEVERAGE=20
GRID_TICK=50
MAX_STAGE=3            # 5차까지 다 가지 않도록 제한 권장
DAILY_LOSS_LIMIT_PCT=  # 킬 스위치 한도
COOLDOWN_MINUTES=30
```